import styled from "styled-components";

export const RequestOwnerModalContentWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 25px;
  font-family: ${(props: any) => props.theme.fonts.$poppins};
`;
